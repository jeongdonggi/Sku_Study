def add(alpha: int, beta: int) -> int:
    return alpha + beta

def substract(alpha: int, beta: int) -> int:
    return alpha - beta

def multiply(alpha: int, beta: int) -> int:
    return alpha * beta

def devide(alpha: int, beta: int) -> tuple:
    return alpha // beta, alpha % beta